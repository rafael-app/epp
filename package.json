{
  "name": "event-planner-pro.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --serve",
    "build": "npx @11ty/eleventy",
    "prebuild": "css-minify -f src/style.css -o public",
    "prestart": "css-minify -f src/style.css -o public",
    "css": "css-minify -f src/style.css -o public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
